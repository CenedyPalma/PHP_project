{
    "require": {
        "php": ">=8.1"
    },
    "scripts": {
        "post-install-cmd": [
            "php bin/console cache:clear --env=prod --no-debug",
            "php bin/console cache:warmup --env=prod --no-debug"
        ]
    }
}