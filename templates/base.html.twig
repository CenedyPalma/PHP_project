<!DOCTYPE html>
<html lang="{{ app.request.locale }}" data-theme="{{ app.user ? app.user.theme : 'light' }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}{{ 'nav.home'|trans }} - Inventory{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>📦</text></svg>">
        
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
        
        <!-- Custom Theme CSS -->
        <link href="/css/theme.css" rel="stylesheet">
        
        {% block stylesheets %}{% endblock %}
        {% block javascripts %}{% block importmap %}{{ importmap('app') }}{% endblock %}{% endblock %}
    </head>
    <body class="d-flex flex-column min-vh-100">
        <!-- Top Navbar (Compact) -->
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary py-2">
            <div class="container-fluid px-3">
                <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
                    <i class="bi bi-box-seam me-2"></i> 
                    <span class="d-none d-sm-inline">Inventory System</span>
                </a>
                
                <!-- Search (center) -->
                <form class="d-none d-md-flex mx-auto" action="{{ path('app_search') }}" method="get" style="max-width: 400px; width: 100%;">
                    <div class="input-group input-group-sm">
                        <input class="form-control" type="search" name="q" placeholder="{{ 'nav.search'|trans }}...">
                        <button class="btn btn-outline-light" type="submit"><i class="bi bi-search"></i></button>
                    </div>
                </form>
                
                <!-- Right side controls -->
                <ul class="navbar-nav ms-auto flex-row align-items-center gap-2">
                    <!-- Theme Toggle -->
                    <li class="nav-item">
                        <a class="nav-link theme-toggle px-2" href="#" onclick="toggleTheme(); return false;">
                            <i class="bi bi-moon-fill" id="themeIcon"></i>
                        </a>
                    </li>
                    
                    <!-- Language -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle px-2" href="#" data-bs-toggle="dropdown">
                            {{ app.request.locale|upper }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="?_locale=en">🇺🇸 English</a></li>
                            <li><a class="dropdown-item" href="?_locale=bn">🇧🇩 বাংলা</a></li>
                        </ul>
                    </li>

                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center px-2" href="#" data-bs-toggle="dropdown">
                                {% if app.user.avatarUrl %}
                                    <img src="{{ app.user.avatarUrl }}" alt="" class="rounded-circle" width="28" height="28">
                                {% else %}
                                    <i class="bi bi-person-circle fs-5"></i>
                                {% endif %}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li class="px-3 py-2 text-muted small">{{ app.user.name }}</li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_user_profile', {id: app.user.id}) }}"><i class="bi bi-person me-2"></i>{{ 'nav.profile'|trans }}</a></li>
                                <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-2"></i>{{ 'nav.logout'|trans }}</a></li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="btn btn-sm btn-outline-light" href="{{ path('app_login') }}">{{ 'nav.login'|trans }}</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </nav>

        <div class="d-flex flex-grow-1">
            <!-- Left Sidebar -->
            <aside class="sidebar bg-body-tertiary border-end d-none d-lg-flex flex-column" style="width: 240px; min-height: calc(100vh - 56px);">
                <nav class="nav flex-column p-3 gap-1">
                    <a class="nav-link sidebar-link d-flex align-items-center gap-2 rounded px-3 py-2 {{ app.request.attributes.get('_route') == 'app_home' ? 'active' : '' }}" href="{{ path('app_home') }}">
                        <i class="bi bi-house"></i> {{ 'nav.home'|trans }}
                    </a>
                    
                    {% if app.user %}
                    <a class="nav-link sidebar-link d-flex align-items-center gap-2 rounded px-3 py-2 {{ app.request.attributes.get('_route') starts with 'app_inventory' ? 'active' : '' }}" href="{{ path('app_inventory_index') }}">
                        <i class="bi bi-collection"></i> {{ 'nav.inventories'|trans }}
                    </a>
                    <a class="nav-link sidebar-link d-flex align-items-center gap-2 rounded px-3 py-2" href="{{ path('app_inventory_index') }}?shared=1">
                        <i class="bi bi-people"></i> Shared With Me
                    </a>
                    {% endif %}
                    
                    <hr class="my-2">
                    
                    {% if app.user %}
                    <a class="nav-link sidebar-link d-flex align-items-center gap-2 rounded px-3 py-2 text-success" href="{{ path('app_inventory_new') }}">
                        <i class="bi bi-plus-circle"></i> {{ 'nav.create'|trans }}
                    </a>
                    {% endif %}
                    
                    {% if is_granted('ROLE_ADMIN') %}
                    <hr class="my-2">
                    <span class="text-muted small px-3 mb-1">Admin</span>
                    <a class="nav-link sidebar-link d-flex align-items-center gap-2 rounded px-3 py-2 {{ app.request.attributes.get('_route') starts with 'app_admin' ? 'active' : '' }}" href="{{ path('app_admin_users') }}">
                        <i class="bi bi-shield-lock"></i> User Management
                    </a>
                    {% endif %}
                </nav>
                
                <!-- Sidebar footer -->
                <div class="mt-auto p-3 border-top small text-muted">
                    <i class="bi bi-info-circle"></i> v1.0.0
                </div>
            </aside>

            <!-- Main Content -->
            <main class="flex-grow-1 p-4" style="overflow-y: auto;">
                {% for type, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ type == 'error' ? 'danger' : type }} alert-dismissible fade show">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endfor %}
                {% endfor %}
                
                {% block body %}{% endblock %}
            </main>
        </div>
        
        <footer class="py-3 border-top" style="background-color: var(--bg-secondary);">
            <div class="container-fluid text-center small" style="color: var(--text-muted);">
                &copy; {{ "now"|date("Y") }} Custom Inventory System
            </div>
        </footer>
        
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        <script>
            function toggleTheme() {
                const html = document.documentElement;
                const current = html.getAttribute('data-theme') || 'light';
                const next = current === 'light' ? 'dark' : 'light';
                html.setAttribute('data-theme', next);
                localStorage.setItem('theme', next);
                updateThemeIcon(next);
            }
            
            function updateThemeIcon(theme) {
                const icon = document.getElementById('themeIcon');
                if (icon) {
                    icon.className = theme === 'dark' ? 'bi bi-sun-fill' : 'bi bi-moon-fill';
                }
            }
            
            // Load saved theme
            document.addEventListener('DOMContentLoaded', function() {
                const saved = localStorage.getItem('theme');
                if (saved) {
                    document.documentElement.setAttribute('data-theme', saved);
                    updateThemeIcon(saved);
                }
            });
        </script>
        {% block scripts %}{% endblock %}
    </body>
</html>
